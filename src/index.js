/**
 * BeltOrganics — entry point.
 *
 * Planned layout:
 *   src/chem/   — chemistry engine (molecules, canonical naming, properties,
 *                 thermodynamics)
 *   src/world/  — belts, reaction chambers, ports, simulation
 */
export const PROJECT_NAME = 'BeltOrganics';

console.log(`${PROJECT_NAME}: project initialized.`);